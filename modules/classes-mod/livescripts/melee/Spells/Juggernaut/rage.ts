export function rageSpell(events: TSEventHandlers) {
    // events.SpellID.OnCast(ID.rage,spell=>{
    //     let caster = spell.GetCaster()
    //     if(caster.GetHealthPct() <= 50){
    //         spell.Finish()
    //     }else{
    //         spell.Cancel()
    //     }
    // })
}