import { std } from "tswow-stdlib";

//
export let DaggerFoK = std.Items.load(2092);
DaggerFoK.Name.enGB.set("Multi-Bladed Dagger");
DaggerFoK.Description.enGB.set(
    "|cffff0000This dagger contains the secrets of Kredesh-kahn, known famously for throwing a shit ton of daggers everywhere... literally everywhere. We had to pull this one out of someones' skull."
);
//
