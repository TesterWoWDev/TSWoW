When using Strings, enter between quotations(s,d) ", '

When dealing with time, the database considers everything in miliseconds.

"check", obviously when checking scripts- "build data" when you're ready to test said scripts(ingame)

"let i=0", create a variable named 'i', set it equal to '0' 

'i' is just a variable.

Example: "For( loop"
"for(let i=0;i<10;i++){
    console.log("words")
}

i<10 = if i is less than 10, repeat the function
i++ = take the variable i, add 1.

"While Loop"
Secondary:
let i = 0;
while(i<10){
    console.log("words")
    i = i +1
}

when